[build]
  command = "npm run build"
  publish = "dist"

# Optional: If you eventually add React Router, uncomment the lines below
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
